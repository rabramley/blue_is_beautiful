[project]
name = "blue-is-beautiful"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mido",
    "python-rtmidi",
    "pyyaml",
    "sortedcontainers",
    "textual",
]
